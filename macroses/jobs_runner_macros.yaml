---
- scm:
    name: runner_scm
    scm:
      - git:
          url: https://github.com/vapolonov/jenkins_conf.git
          credentials-id: jenkins
          branches:
            - FETCH_HEAD
          refspec: ${REFSPEC}
